server:
  port: 8081

spring:
  application:
    name: ticket-service

  datasource:
    url: jdbc:mysql://localhost:3308/ticketing_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true